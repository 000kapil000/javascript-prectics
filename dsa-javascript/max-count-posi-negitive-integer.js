// que: max countg of positive or nagative integer 
// ggiven an array nums sorted in non decreasing order, return the maximum b/w the number of positive integers and the numbers of negative integers

// input : [-2,-1,-1,1,2,3 ] output : 3
// explation : there are 3 positive integer and 3 negative integer the maximum count them is 3

function maximumCount(){
return Math.max(upperBound(nums),lowerBounds(nums));
}

function upperBound(){

}

function lowerBounds(){

}